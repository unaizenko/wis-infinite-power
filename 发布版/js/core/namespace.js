(function bootstrapWISNamespace(global) {
  "use strict";

  const WIS = global.WIS = global.WIS || {};
  WIS.Core = WIS.Core || {};
  WIS.Power = WIS.Power || {};
  WIS.Cultivation = WIS.Cultivation || {};
  WIS.Meta = WIS.Meta || {};
  WIS.UI = WIS.UI || {};
}(window));
